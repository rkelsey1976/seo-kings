# Saltford area page images

Images here are used on `/areas/saltford` (hero and local shots). Same style as Keynsham area page; section images (wireframe, laptop, coding) are shared from `/images/keynsham/`.

## Current files

- **saltford-hero.jpg** – Hero background (Saltford Marina, River Avon). In use.

## Optional

Add more Saltford landmark images for the gallery (e.g. Saltford Manor, Brass Mill) — see Wikimedia Commons for geograph.org.uk shots. Save here and reference in `AreaDetail.jsx` under the `saltford` area `gallery` array.
